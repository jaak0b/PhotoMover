<UserControl x:Class="PhotoMover.Views.SettingsUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PhotoMover.Views"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300" Name="SettingsUc">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Source Folder:" Margin="5" Padding="5" />
        <TextBox Grid.Row="0" Grid.Column="1" Margin="5" Padding="5" IsReadOnly="True"
                 Text="{Binding ElementName=SettingsUc, Path=AppConfig.FolderSource, Mode=TwoWay}" />
        <Button Grid.Row="0" Grid.Column="2" Margin="5" Padding="5" Content="Select" Name="SelectFolderSourceButton"
                Click="SelectFolderSourceButton_OnClick" />

        <Label Grid.Row="1" Grid.Column="0" Content="Target Folder:" Margin="5" Padding="5" />
        <TextBox Grid.Row="1" Grid.Column="1" Margin="5" Padding="5" IsReadOnly="True"
                 Text="{Binding ElementName=SettingsUc, Path=AppConfig.FolderTarget, Mode=TwoWay}" />
        <Button Grid.Row="1" Grid.Column="2" Margin="5" Padding="5" Content="Select" Name="SelectFolderTargetButton"
                Click="SelectFolderTargetButton_OnClick" />

        <Label Grid.Row="2" Grid.Column="0" Content="File Pattern:" Margin="5" Padding="5" ToolTip="Allows for filtering of files in the source folder that should be moved. Use * if you want to move all files." />
        <TextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Margin="5" Padding="5"
                 Text="{Binding ElementName=SettingsUc, Path=AppConfig.FilePattern, Mode=TwoWay}" />

        <Label Grid.Row="3" Grid.Column="0" Content="Group Files Pattern:" Margin="5" Padding="5" ToolTip="Allows for grouping files that are moved to the target folder. Check Github for examples and more details." />
        <TextBox Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" Margin="5" Padding="5"
                 Text="{Binding ElementName=SettingsUc, Path=AppConfig.FolderPattern, Mode=TwoWay}" />
        
        

    </Grid>
</UserControl>