<UserControl x:Class="PhotoMover.Views.MainWindowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PhotoMover.Views"
             xmlns:viewModel="clr-namespace:PhotoMover.ViewModel"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300" Name="UserControl">
    <UserControl.DataContext>
       <viewModel:MainWindowViewModel></viewModel:MainWindowViewModel>
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="AUTO"/>
            <RowDefinition Height="AUTO"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ItemsControl ItemsSource="{Binding Presets}" HorizontalAlignment="Left">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" BorderThickness="2" Padding="2" Margin="2" CornerRadius="5">
                        <Grid HorizontalAlignment="Left">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition SharedSizeGroup="PresetsColumn1" Width="Auto"/>
                                <ColumnDefinition SharedSizeGroup="PresetsColumn2" Width="*"/>
                                <ColumnDefinition SharedSizeGroup="PresetsColumn3" Width="Auto"/>
                                <ColumnDefinition SharedSizeGroup="PresetsColumn4" Width="Auto"/>
                                <ColumnDefinition SharedSizeGroup="PresetsColumn5" Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition SharedSizeGroup="PresetsRow1" Height="Auto"/>
                                <RowDefinition SharedSizeGroup="PresetsRow2" Height="1"/>
                                <RowDefinition SharedSizeGroup="PresetsRow3" Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Label Content="{Binding Name}" Padding="2" Margin="2" FontWeight="Bold"></Label>
                            <Button Grid.Row="0" Grid.Column="2" CommandParameter="{Binding}" Command="{Binding DataContext.OpenPresetSettingsCommand, RelativeSource={RelativeSource  AncestorType=UserControl }}" Padding="2" Margin="2" >
                                <iconPacks:PackIconBootstrapIcons Kind="Gear" />
                            </Button>
                            <Button Grid.Row="0" Grid.Column="4" CommandParameter="{Binding}" Command="{Binding DataContext.RunPresetCommand, RelativeSource={RelativeSource  AncestorType=UserControl }}" Padding="2" Margin="2" >
                                <iconPacks:PackIconBootstrapIcons Kind="PlayFill" />
                            </Button>
                            <Border Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="5" BorderBrush="Black" BorderThickness="1" HorizontalAlignment="Stretch" Padding="2,0" ></Border>
                            
                            <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal">
                                <Label Content="{Binding SourceFolder}" VerticalAlignment="Center"/>
                                <iconPacks:PackIconBootstrapIcons Kind="ArrowRight" VerticalAlignment="Center" />
                                <Label Content="{Binding DestinationFolder}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                        <Border.ContextMenu>
                            <ContextMenu DataContext="{Binding DataContext, RelativeSource={RelativeSource AncestorType=Border}}">
                                <MenuItem Header="Delete" Click="MenuItem_OnClick" >
                                    <MenuItem.Icon>
                                        <iconPacks:PackIconBootstrapIcons Kind="Trash" />
                                    </MenuItem.Icon>
                                </MenuItem>
                            </ContextMenu>
                        </Border.ContextMenu>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Button Grid.Row="1" Command="{Binding AddPresetCommand}" Padding="2" Margin="2" HorizontalContentAlignment="Left" BorderThickness="0" Background="Transparent" >
            <iconPacks:PackIconBootstrapIcons Kind="Plus" />
        </Button>
        
        <DataGrid Grid.Row="2" ItemsSource="{Binding Tasks}" >
            
            
        </DataGrid>
    </Grid>
</UserControl>